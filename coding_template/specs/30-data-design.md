# Data Design Spec 模板

> 目的：定义数据模型、数据链路、快照和审计规则。

## 核心数据闭环

```text
[数据源] → [采集/标准化] → [存储] → [读取/缓存] → [API] → [前端展示] → [用户交互] → [回写]
```

## 实体关系

```text
[实体 A] 1 --- n [实体 B]
[实体 B] n --- 1 [实体 C]
```

## 表定义

### [表名]

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | [类型] | primary key | 主键 |

## 索引

| 表 | 字段 | 类型 | 目的 |
| --- | --- | --- | --- |
| [表] | [字段] | [索引类型] | [目的] |

## 数据处理规则

- Agent / 后台任务不直接访问外部数据源。
- 数据缺失时必须输出 data_gap，不能编造。
- 每个处理步骤必须记录输入、输出、耗时和状态。

## 快照与审计

- 需要快照的数据：[说明]
- 快照创建和读取规则：[说明]
- 需要审计的操作：[说明]
- 删除策略：[物理删除 / 逻辑删除 / 禁止删除]

## 验证要求

数据链路验收必须记录真实证据：关键 ID、接口返回、数据库记录、前端展示状态。
